version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - app_data:/app/data
      - chroma_data:/app/chroma_data
    restart: unless-stopped

volumes:
  app_data:
  chroma_data:
